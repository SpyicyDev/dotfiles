#!/bin/sh
# Todoist due-today counter for sketchybar — DISARMED 2026-09-01.
# The todoist-api-token 1Password item went dead (401) and was archived at
# Mack's ask; sketchybar isn't running on this machine anymore. If sketchybar
# returns: mint a new token (Todoist → Settings → Integrations → Developer),
# store it as op://Developer/todoist-api-token/credential, and restore this
# plugin from git history (pre-2026-09-01).
sketchybar --set "$NAME" label="–" 2>/dev/null || true
